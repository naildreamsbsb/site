const CLIENT_ROLE="cliente",TEMP_BOOKING_KEY="agendamento_temp",STUDIO_WHATSAPP_NUMBER="5561985879423";
const card=document.querySelector("#confirmation-card"),message=document.querySelector("#confirmation-message"),confirmButton=document.querySelector("#confirm-booking-button"),whatsappFallback=document.querySelector("#whatsapp-fallback-button");let booking,client;
function showMessage(text="",type="error"){message.textContent=text;message.className=text?`message ${type}`:"message"}
function readBooking(){try{const value=JSON.parse(localStorage.getItem(TEMP_BOOKING_KEY));return Array.isArray(value?.itens)&&value.itens.length&&value.data&&value.hora_inicio?value:null}catch{return null}}
function money(value){return Number(value).toLocaleString("pt-BR",{style:"currency",currency:"BRL"})}
function dateText(value){const[y,m,d]=value.split("-");return`${d}/${m}/${y}`}
async function context(){const session=await supabaseClient.auth.getSession();if(session.error)throw session.error;if(!session.data.session){location.replace("login.html");return false}const[profile,result]=await Promise.all([supabaseClient.from("profiles").select("role").eq("id",session.data.session.user.id).maybeSingle(),supabaseClient.from("clientes").select("id,full_name,phone").eq("profile_id",session.data.session.user.id).maybeSingle()]);if(profile.error)throw profile.error;if(result.error)throw result.error;if(profile.data?.role!==CLIENT_ROLE||!result.data)throw new Error("Não foi possível localizar o cadastro da cliente.");client=result.data;return true}
function render(){const list=document.querySelector("#summary-procedures");booking.itens.forEach((item,index)=>{const row=document.createElement("article");const title=document.createElement("strong");title.textContent=`${index+1}. ${item.servico_nome}`;const detail=document.createElement("span");detail.textContent=`${item.profissional_nome} · ${item.duracao_minutos} min · ${money(item.valor)}`;row.append(title,detail);list.appendChild(row)});document.querySelector("#summary-date").textContent=dateText(booking.data);document.querySelector("#summary-start-time").textContent=booking.hora_inicio;document.querySelector("#summary-end-time").textContent=booking.hora_fim;const duration=booking.itens.reduce((sum,item)=>sum+Number(item.duracao_minutos),0),price=booking.itens.reduce((sum,item)=>sum+Number(item.valor),0);document.querySelector("#summary-duration").textContent=`${duration} min`;document.querySelector("#summary-price").textContent=money(price);card.hidden=false}
function startAt(){const[y,m,d]=booking.data.split("-").map(Number),[hour,minute]=booking.hora_inicio.split(":").map(Number),desired=Date.UTC(y,m-1,d,hour,minute);const formatter=new Intl.DateTimeFormat("en-CA",{timeZone:"America/Sao_Paulo",year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",second:"2-digit",hourCycle:"h23"});let timestamp=desired;for(let attempt=0;attempt<2;attempt+=1){const parts=Object.fromEntries(formatter.formatToParts(new Date(timestamp)).map(part=>[part.type,part.value]));const displayed=Date.UTC(Number(parts.year),Number(parts.month)-1,Number(parts.day),Number(parts.hour),Number(parts.minute),Number(parts.second));timestamp=desired-(displayed-timestamp)}return new Date(timestamp).toISOString()}
function whatsappUrl(){const procedures=booking.itens.map((item,index)=>`${index+1}. ${item.servico_nome} — ${item.profissional_nome}`).join("\n");const text=`Novo agendamento Nail Dreams 💕\n\nCliente: ${client.full_name}\n${procedures}\nData: ${dateText(booking.data)}\nHorário: ${booking.hora_inicio} às ${booking.hora_fim}`;return`https://wa.me/${STUDIO_WHATSAPP_NUMBER}?text=${encodeURIComponent(text)}`}
function showWhatsAppFallback(url){whatsappFallback.hidden=false;whatsappFallback.onclick=()=>{const opened=window.open(url,"_blank");if(opened)opened.opener=null;else location.assign(url)}}
async function confirm(){const whatsappWindow=window.open("","_blank");if(whatsappWindow)whatsappWindow.opener=null;confirmButton.disabled=true;confirmButton.textContent="Criando agendamento...";whatsappFallback.hidden=true;showMessage();try{const{data,error}=await supabaseClient.rpc("solicitar_agendamento_recepcao_v2",{p_cliente_id:client.id,p_start_at:startAt(),p_itens:booking.itens.map((item,index)=>({servico_id:item.servico_id,profissional_id:item.profissional_id,ordem:index+1})),p_cliente_nome:null,p_cliente_phone:null,p_cliente_email:null,p_appointment_type:"normal",p_requires_deposit_override:null,p_deposit_percent_override:null,p_notes:null});if(error)throw error;if(data?.success!==true)throw new Error(data?.message||"Não foi possível criar o agendamento.");const url=whatsappUrl();showMessage(data.message||"Agendamento criado com sucesso.","success");localStorage.removeItem(TEMP_BOOKING_KEY);confirmButton.hidden=true;document.querySelector(".confirmation-note").textContent="Agendamento registrado. Nossa equipe dará continuidade ao seu atendimento. 💗";if(whatsappWindow&&!whatsappWindow.closed)whatsappWindow.location.href=url;else showWhatsAppFallback(url)}catch(error){if(whatsappWindow&&!whatsappWindow.closed)whatsappWindow.close();showMessage(error.message||"Não foi possível criar o agendamento.");confirmButton.disabled=false;confirmButton.textContent="Tentar novamente"}}
async function init(){booking=readBooking();if(!booking)return location.replace("agendar.html");try{if(await context()){render();confirmButton.addEventListener("click",confirm)}}catch(error){showMessage(error.message||"Não foi possível preparar a confirmação.")}}init();
